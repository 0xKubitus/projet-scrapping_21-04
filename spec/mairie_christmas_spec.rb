require_relative '../lib/mairie_christmas'

describe "the ... method" do
  it "should ..." do
    expect(...(x)).to eq(true)
    
  end

  it "should ..." do
    expect(...(x)).to eq("quelque chose")
    expect(...(x)).to eq("quelque chose")
  end
end

describe "the ... method" do
  it "should ..." do
    expect(...(x)).to eq(true)
  end
end
